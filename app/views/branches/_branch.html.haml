= form_for branch, remote: true do |f|
  = hidden_field_tag :repository_id, @repository.id
  .row.branch-item
    = image_tag 'logo.png'
    = branch.name
    .pull-right
      = f.check_box :watched
